name: PrintOnPullToMain
run-name: ${{ github.event_name }} by ${{ github.actor }} in ${{ github.path }}
on:
  pull_request:
jobs:
  CheckForFiles:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: if test -f "testwf.txt";
             then
                echo "testwf.txt found!";
             else
                echo "testwf.txt missing!";
                exit 123;
             fi
